backend = "glx";
vsync = true;


corner-radius = 15
rounded-corners-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
];

opacity-rule = [
"90:class_g = 'st-256color' && focused",
"95:class_g = 'st-256color' && !focused"
];
